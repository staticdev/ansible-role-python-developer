---
roles:
  - name: avanov.pyenv
    version: 1.1.0
  - name: oefenweb.pycharm
    version: v5.0.7
    when: "{{ install_pycharm }}"
  - name: gantsign.visual-studio-code
    version: 6.6.0
    when: "{{ install_vscode }}"
